apply plugin: 'com.android.application'

android {
    signingConfigs {
        AcdnitKeyStore {
            keyAlias 'AcdnitKeyStore'
            keyPassword 'Acdntvhaa@3012'
            storeFile file('D:/Documents/CTWassco/AcdnitKeyStore.jks')
            storePassword 'Acdntvhaa@3012'
        }
    }
    compileSdkVersion 26
    defaultConfig {
        applicationId "com.tvhan.accountkitdemo.app"
        minSdkVersion 15
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"


    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    implementation project(':feature')
    implementation project(':base')
}
